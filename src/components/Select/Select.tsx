import React from 'react';

type ItemType = {
  title: string;
  value: any;
};

type SelectPropsType = {
  value: any;
  items: ItemType[];
};

export const Select = (props: SelectPropsType) => {
  return <div>{}</div>;
};
